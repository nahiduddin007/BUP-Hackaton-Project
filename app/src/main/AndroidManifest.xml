<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kingscoder.clopirox_without_signup">

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoctorDetails"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".CalenderViewActivity" />
        <activity
            android:name=".BMIActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EmbulenceActivity" />
        <activity
            android:name=".ViccineMainActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <receiver android:name=".NotificationPublisher" />

        <activity
            android:name=".AddItem"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EditItem"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddCategory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MedicinePageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/SplashScreenTheme" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>